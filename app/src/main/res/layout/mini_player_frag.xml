<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <import type="android.view.View" />

        <import type="com.zaze.tribe.music.MusicPlayerRemote" />

    </data>

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:foreground="@drawable/rect_selector"
        android:orientation="vertical"
        android:visibility="@{MusicPlayerRemote.curMusicData == null ? View.GONE : View.VISIBLE}">

        <ProgressBar
            android:id="@+id/miniPlayerProgressBar"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="2dp"
            android:progressDrawable="@drawable/progress_horizontal" />

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="4dp">

            <androidx.cardview.widget.CardView
                android:id="@+id/miniPlayerAlbumCard"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginLeft="4dp"
                android:layout_marginRight="4dp"
                app:cardCornerRadius="4dp"
                app:cardElevation="0dp">

                <ImageView
                    android:layout_width="@dimen/album_cover_size_mini"
                    android:layout_height="@dimen/album_cover_size_mini"
                    android:contentDescription="@null"
                    android:scaleType="fitCenter"
                    app:bmp="@{MusicPlayerRemote.curMusicData}"
                    tools:src="@mipmap/ic_launcher" />
            </androidx.cardview.widget.CardView>

            <LinearLayout
                android:id="@+id/miniPlayerMenuLayout"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentEnd="true"
                android:layout_alignParentRight="true"
                android:layout_centerVertical="true"
                android:orientation="horizontal">

                <androidx.appcompat.widget.AppCompatImageView
                    android:id="@+id/miniPlayerStatus"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginEnd="4dp"
                    android:layout_marginRight="4dp"
                    android:onClick="@{()-> MusicPlayerRemote.start()}"
                    android:src="@{MusicPlayerRemote.isPlaying?@drawable/ic_pause_circle_outline_black_24dp : @drawable/ic_play_circle_outline_black_24dp}"
                    tools:src="@drawable/ic_play_circle_outline_black_24dp" />

                <androidx.appcompat.widget.AppCompatImageView
                    android:id="@+id/miniPlayerListIv"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginLeft="4dp"
                    android:layout_marginRight="4dp"
                    android:src="@drawable/ic_more_vert_black_24dp" />
            </LinearLayout>

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_centerVertical="true"
                android:layout_toStartOf="@id/miniPlayerMenuLayout"
                android:layout_toLeftOf="@id/miniPlayerMenuLayout"
                android:layout_toEndOf="@id/miniPlayerAlbumCard"
                android:layout_toRightOf="@id/miniPlayerAlbumCard"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/miniPlayerTitle"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:ellipsize="end"
                    android:maxLength="14"
                    android:maxLines="1"
                    android:text="@{MusicPlayerRemote.curMusicData.title}"
                    android:textColor="#333333"
                    android:textSize="14sp"
                    tools:text="极乐净土极乐净土极乐净土极乐净土极乐净土" />

                <TextView
                    android:id="@+id/miniPlayerArtist"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="4dp"
                    android:ellipsize="end"
                    android:maxLines="1"
                    android:text="@{MusicPlayerRemote.curMusicData.artistName}"
                    android:textSize="12sp"
                    tools:text="MARIA" />

            </LinearLayout>

        </RelativeLayout>

    </LinearLayout>
</layout>
