<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <variable
            name="music"
            type="com.zaze.tribe.data.dto.MusicInfo" />

        <variable
            name="viewModel"
            type="com.zaze.tribe.music.MusicViewModel" />

    </data>

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:clickable="true"
        android:focusable="true"
        android:foreground="@drawable/rect_selector"
        android:gravity="center_vertical"
        android:onClick="@{()-> viewModel.start(music)}"
        android:orientation="horizontal">


        <android.support.v7.widget.CardView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/dp_8"
            app:cardCornerRadius="@dimen/dp_4"
            app:cardElevation="0dp">

            <ImageView
                android:id="@+id/music_item_album_iv"
                android:layout_width="@dimen/dp_48"
                android:layout_height="@dimen/dp_48"
                android:contentDescription="@null"
                android:scaleType="fitCenter"
                app:bmp="@{music}"
                tools:src="@mipmap/ic_launcher" />
        </android.support.v7.widget.CardView>


        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <RelativeLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:paddingTop="@dimen/dp_4"
                android:paddingBottom="@dimen/dp_4">

                <ImageView
                    android:id="@+id/music_item_more_iv"
                    android:layout_width="@dimen/dp_54"
                    android:layout_height="@dimen/dp_54"
                    android:layout_alignParentEnd="true"
                    android:layout_alignParentRight="true"
                    android:layout_centerVertical="true"
                    android:contentDescription="@null"
                    android:onClick="@{()-> viewModel.showMore(music)}"
                    android:scaleType="center"
                    android:src="@drawable/ic_more_vert_black_24dp" />


                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_centerInParent="true"
                    android:layout_toStartOf="@id/music_item_more_iv"
                    android:layout_toLeftOf="@id/music_item_more_iv"
                    android:orientation="vertical">

                    <TextView
                        android:id="@+id/music_item_title_tv"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:ellipsize="end"
                        android:maxLines="1"
                        android:text="@{music.title}"
                        android:textColor="#333333"
                        android:textSize="@dimen/sp_16"
                        tools:text="极乐净土" />

                    <TextView
                        android:id="@+id/music_item_artist_tv"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="@dimen/dp_4"
                        android:ellipsize="end"
                        android:maxLines="1"
                        android:text="@{music.artistName}"
                        android:textSize="@dimen/sp_14"
                        tools:text="MARIA" />

                </LinearLayout>

            </RelativeLayout>

            <View
                android:layout_width="match_parent"
                android:layout_height="@dimen/dp_1"
                android:layout_gravity="bottom"
                android:layout_marginTop="@dimen/dp_4"
                android:background="@color/main_split_line" />
        </FrameLayout>

    </LinearLayout>
</layout>
